import org.gradle.api.initialization.resolve.RepositoriesMode

pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()

    }
}

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        maven {
            name = "GitHubPackages"
            url = uri("https://maven.pkg.github.com/TECH7Fox/myolink")
            credentials {
                username = properties.get("gpr.user") ?: System.getenv("USERNAME")
                password = properties.get("gpr.key") ?: System.getenv("TOKEN")
            }
        }
        google()
        mavenCentral()
    }
}

rootProject.name = "Myo Assistant"
include ':app'
